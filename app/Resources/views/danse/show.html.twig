{% extends 'base.html.twig' %}

{% block body %}
    <h1>Danse</h1>

    <table>
        <tbody>
            <tr>
                <th>Nomdanse</th>
                <td>{{ danse.nomdanse }}</td>
            </tr>
            <tr>
                <th>Iddanse</th>
                <td>{{ danse.iddanse }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('danse_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('danse_edit', { 'id': danse.iddanse }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
